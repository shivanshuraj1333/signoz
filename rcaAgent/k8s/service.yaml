apiVersion: v1
kind: Service
metadata:
  name: rca-agent
  labels:
    app: rca-agent
spec:
  selector:
    app: rca-agent
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP 